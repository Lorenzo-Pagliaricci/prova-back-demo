# ${{ values.name }} - Service Documentation

This documentation is generated by the Backstage template and versioned in Git.

## What this service does

- Service name: `${{ values.name }}`
- Type: Spring Boot REST service
- Group ID: `${{ values.mavenGroupId }}`
- Main endpoints:
  - `GET /api/hello`
  - `GET /actuator/health`

## Platform integrations generated by template

- GitLab project: `${{ values.gitlabProjectPath }}`
- Jenkins pipeline job: `${{ values.jenkinsJobFullName }}`
- Nexus repository: `${{ values.nexusRepositoryName }}`

## Next steps

1. Update this page with the real business purpose of the service.
2. Add API contracts and operational notes.
3. Keep docs updated at every release.
